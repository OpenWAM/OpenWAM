name: parallel_stream_libero_lingbot_m1_fastwam_first_frame

data:
  # Dataset source knobs
  # - pick the dataset family and where samples/latents live locally
  dataset_name: libero
  dataset_type: lerobot_v2_latent_local
  local_root: "${paths.datasets.libero_root}"
  replay_status_path: "${paths.datasets.libero_replay_status_path}"
  replay_status_policy: successful_only
  val_replay_status_policy: failure_only
  require_replay_status: true
  val_require_replay_status: true
  empty_text_embedding_path: "${paths.datasets.empty_text_embedding}"
  split: train
  # View + latent layout knobs
  # - camera_names drive RGB metadata, latent_camera_names drive latent lookup
  camera_names: [observation.images.agentview_rgb, observation.images.eye_in_hand_rgb]
  latent_camera_names: [observation.images.agentview_rgb, observation.images.eye_in_hand_rgb]
  canonical_height: 128
  canonical_width: 256
  num_frames: 4
  frame_stride: 1
  sample_stride: 1
  episode_cache_size: 2
  train_fraction: 1.0
  split_seed: 0
  # FastWAM first-frame samples one random 4-latent-frame window.
  # The first latent frame is the visual/proprio anchor; video loss supervises
  # future latent frames and action loss supervises the corresponding 16 actions.
  sample_construction:
    mode: uniform_segment
    segment_min_frames: 4
    segment_max_frames: 4
    segment_length_stride: 4
    randomize_segment_length: false
    randomize_segment_start: true
    require_full_segment: true
    # FastWAM conditions on the first raw frame/state of the sampled window.
    state_anchor_mode: sample_start_frame
    chunk_size: 4
    window_size: 4
    randomize_geometry: false
    sample_weight_mode: uniform
  train_batch_size: 1
  val_batch_size: 1
  num_workers: 8
  # Canonical canvas placement knobs
  # - each source camera is placed into one fixed region of the shared canvas
  view_layout:
    - source_name: observation.images.agentview_rgb
      canonical_name: image
      top: 0
      left: 0
      height: 128
      width: 128
    - source_name: observation.images.eye_in_hand_rgb
      canonical_name: wrist_image
      top: 0
      left: 128
      height: 128
      width: 128
  # Action/state schema knobs
  action_schema:
    action_dim: 7
    action_horizon: 16
    state_dim: 8
    state_horizon: 1
  # Action-target encoding knobs
  # - representation chooses the target family
  # - *_representation fields choose how pose/gripper state is encoded
  action_target:
    representation: raw
    source_key: action
    pose_source_key: observation.state
    state_encoding: eef_pos_axisangle_gripper_2d
    reference_source: anchor_state
    rotation_representation: axis_angle
    include_gripper: true
    gripper_representation: action_command
    gripper_action_index: -1

backbone:
  # Shared transformer identity + attention knobs
  implementation: shared_transformer
  # FastWAM first-frame uses a dedicated dense-mask manual forward path.
  train_attn_mode: torch
  infer_attn_mode: torch
  hidden_size: 3072
  num_layers: 30
  num_heads: 24
  attention_head_dim: 128
  ffn_dim: 14336
  # Reference asset loading knobs
  # - pretrained_model_name_or_path points at the base LingBot-VA assets
  # - transformer_subdir can point at a posttrain checkpoint transformer export
  pretrained_model_name_or_path: "${paths.models.lingbot_va_base}"
  transformer_subdir: "${paths.checkpoints.parallel_stream_exact_libero_step_400}/transformer"
  max_text_tokens: 512
  load_wan_vae_frontend: true
  load_text_conditioning: true
  reference_assets_device_policy: cpu_offload

policy_variant:
  # FastWAM first-frame runtime knobs.
  # - training denoises the full video window but keeps frame 0 clean and masks
  #   frame-0 video loss, matching FastWAM first-frame conditioning
  # - action tokens attend only first-frame video tokens plus action tokens
  # - rollout is action-only: each chunk re-encodes the current RGB observation
  # - this is FastWAM joint training with action-only rollout, not action-only training
  name: parallel_stream
  runtime_mode: fastwam_first_frame
  reference_profile: libero
  hidden_size: 3072
  frame_chunk_size: 4
  action_per_frame: 4
  # Unused by this no-cache rollout; train-time FastWAM mask is explicit.
  attn_window: 30
  sequence_order: [video_noisy, video_condition, action_noisy, action_condition]
  mask_mode: lingbot_chunked
  cache_mode: metadata_only
  preserve_video_pretrain_history: false
  # Require explicit single-frame condition_latents exported by the augment script.
  require_condition_latents: true
  proprio_context_mode: per_chunk_additive
  temporal_position_mode: local_zero_based

action_decoder:
  # Decoder surface knobs
  # - for exact method 1 this stays on the LingBot-compatible parallel decoder
  name: lingbot_parallel_decoder
  hidden_size: 3072
  action_dim: 30
  action_horizon: 16

training:
  # Diffusion training knobs
  # - timestep counts and sigma shifts for video/action noise schedules
  video_num_train_timesteps: 1000
  action_num_train_timesteps: 1000
  video_sigma_shift: 5.0
  action_sigma_shift: 1.0
  use_teacher_forcing: false
  chunk_size: 4
  # Unused by the FastWAM mask; kept explicit for config compatibility.
  window_size: 64
  # Optimization knobs
  # - optimizer_name / scheduler_name pick the optimizer family and LR schedule
  # - warmup, accumulation, and clipping control update behavior
  optimizer_name: adamw
  scheduler_name: constant_with_warmup
  learning_rate: 1.0e-5
  beta1: 0.9
  beta2: 0.95
  weight_decay: 0.1
  warmup_steps: 10
  gradient_accumulation_steps: 10
  max_grad_norm: 2.0
  num_steps: 5000
  text_condition_dropout_prob: 0.1
  # Supervision knobs
  # - enabled_objectives turns objective families on/off
  # - *_loss_weight scales each enabled objective without changing the runtime
  enabled_objectives: [latent, action]
  latent_loss_weight: 1.0
  action_loss_weight: 1.0
  # Trainability knobs
  # - choose which component families receive gradients
  trainable_components: [visual_tower.runtime_backbone]
  # FastWAM first-frame trains future-video and action objectives from one anchor image.
  sample_loss_weight_mode: none

inference:
  # Rollout/inference knobs
  # - frame_chunk_size and *_num_inference_steps shape the denoising rollout
  # - video CFG/denoising is disabled at rollout; this mode predicts actions only
  video_num_inference_steps: 20
  action_num_inference_steps: 50
  frame_chunk_size: 4
  use_cache: false
  guidance_scale: 1.0
  action_guidance_scale: 1.0
  video_exec_step: -1

trainer:
  # Loop + runtime knobs
  # - runtime chooses the training engine
  # - batch_adapter chooses RGB-view vs latent-native training inputs
  # - loop_policy chooses epoch-based vs step-based training
  # - strategy chooses the launch/distribution backend
  max_epochs: 1
  limit_train_batches: null
  limit_val_batches: 0
  log_every_n_steps: 1
  accelerator: gpu
  devices: 1
  precision: bf16-mixed
  enable_checkpointing: true
  enable_model_summary: false
  runtime: composable
  batch_adapter: latents
  loop_policy: steps
  strategy: fsdp
  # Checkpoint + export knobs
  save_interval: 100 #50
  checkpoint_mode: full_training_state
  export_runtime_backbone: true
  # Logging + experiment tracking knobs
  enable_jsonl_logging: true
  enable_wandb: true
  wandb_project: openwam-method1-libero
  wandb_mode: online
