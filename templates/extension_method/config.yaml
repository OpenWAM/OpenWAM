name: template_method_smoke

data:
  dataset_name: public_tiny
  dataset_type: synthetic_multiview
  camera_names: [camera_0]
  canonical_height: 64
  canonical_width: 64
  num_frames: 2
  action_schema:
    action_dim: 4
    action_horizon: 2
    state_dim: 3
    state_horizon: 1

backbone:
  implementation: dummy
  hidden_size: 32
  num_layers: 1
  num_heads: 4

policy_variant:
  name: replace_with_new_policy_variant
  hidden_size: 32
  attach_site: post_visual_core

action_decoder:
  name: replace_with_new_action_decoder
  hidden_size: 32
  action_dim: 4
  action_horizon: 2
